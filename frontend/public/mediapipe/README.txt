Place local MediaPipe runtime assets here for offline or intranet deployment.

Expected optional paths:
- /mediapipe/tasks-vision/index.js
- /mediapipe/tasks-vision/wasm/*
- /mediapipe/models/pose_landmarker_lite.task

If these files are absent, the frontend falls back to the online CDN model.
